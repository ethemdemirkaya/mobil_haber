{
  "_comment": "Bu dosyayı `.env.json` olarak kopyalayıp anahtarınızı doldurun. .env.json gitignored — kazara commit edilmez. VSCode F5 veya `pnpm run`/`./run.ps1` ile otomatik kullanılır.",
  "OPENROUTER_API_KEY": ""
}
